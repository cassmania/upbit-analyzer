# 코인 분석 프롬프트 V3.1 사이트 이식 계획

## 현재 구조

이 사이트는 LLM API에 프롬프트를 보내는 앱이 아니라, 브라우저에서 거래소 OHLCV를 직접 계산하는 규칙 기반 분석기다. 따라서 프롬프트 문장만 저장하지 않고 검증 가능한 규칙을 계산·표시·브리핑 생성 로직으로 이식한다.

## 구현 범위

1. 기존 완료봉 분리와 종목별 상태 격리를 유지한다.
2. TA 엔진에 ADX(14)와 SMA50을 추가하고 기존 지표와 호환되게 노출한다.
3. 별도 V3 분석 모듈에 확인 피벗(좌우 5봉), BOS, FVG, 오더블록 후보, 피보나치 기준 스윙을 구현한다.
4. 화면에 분석 기준·추세/유동성·모멘텀·파생상품·펀더멘털 데이터 가용성·최종 실행 판단을 통합한 V3 패널을 추가한다.
5. 기존 브리핑을 V3.1 형식으로 교체해 USDT/KRW, 출처 시각, 확인 불가 데이터, 롱·숏·관망 조건을 함께 복사할 수 있게 한다.
6. 신규 모듈 회귀 테스트와 기존 97개 테스트를 모두 실행한다.
7. 변경 파일만 커밋해 `main`에 푸시하고 Vercel 자동 배포 상태와 공개 URL을 확인한다.

## 정확도·안전 기준

- 기술 지표와 SMC는 마지막 확정 봉만 사용한다.
- 선택 종목과 거래소의 캔들만 입력으로 받으며 이전 종목 결과를 재사용하지 않는다.
- OHLCV만으로 확인할 수 없는 뉴스·언락·고래 흐름·청산맵·CVD·온체인은 `현재 실시간 데이터 확인 불가`로 표시한다.
- VPVR은 체결별 원자료가 아닌 OHLCV 범위분배 근사임을 명시한다.
- 백테스트 없는 승률·상승 확률을 표시하지 않는다.
- 신호는 발동 조건과 무효화 가격을 함께 제공하며 관망을 정상 결과로 취급한다.

## 완료 조건

- 신규 V3 모듈과 기존 회귀 테스트가 모두 통과한다.
- 브리핑과 화면이 같은 종목·같은 계산 결과를 사용한다.
- Git 푸시가 성공하고 Vercel 배포가 Ready 상태가 된다.
