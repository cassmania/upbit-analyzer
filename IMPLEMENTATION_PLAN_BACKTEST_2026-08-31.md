# upbit-analyzer 지표 정확도 및 신호 백테스트 계획

## 목표

현재 배포 중인 `upbit-analyzer`의 기술지표 계산을 독립 기준값과 대조하고, 화면의 규칙 기반 롱·숏 타점이 비용 반영 후에도 재현 가능한 성과를 보이는지 검증한다. 오류가 확인된 부분만 최소 수정하며, 검색·즐겨찾기·거래소 선택·마지막 화면 복원 기능은 보존한다.

## 대상과 기준

- 저장소: `E:\AI 관련 자료\클로드\upbit_analyzer`
- 공개 페이지: `https://upbit-analyzer.vercel.app/`
- 기준 시장: 현재 기본값과 같은 Binance 현물 USDT
- 종목: BTCUSDT, ETHUSDT, BNBUSDT, XRPUSDT, SOLUSDT
- 검증 기간: 2024-01-01 UTC부터 실행 시점의 마지막 확정 1시간봉까지
- 지표 입력: 진행 중인 봉 제외, 각 판단 시점 이전의 확정봉만 사용
- 신호 계산: 현재 `TAEngine`, `LevelEngine`, `SignalEngine` 규칙 그대로 재사용

## 백테스트 체결 가정

- 신호 판단: 1시간봉 확정 직후
- 진입: 다음 1시간봉에서 계획 진입가에 도달했을 때만 체결
- 청산: 신호가 제시한 구조적 손절 또는 1차 목표
- 최대 보유: 48시간
- 동일 봉에서 손절·목표 동시 도달: 손절 우선
- 비용: Binance 현물 기준 수수료 편도 0.10%, 슬리피지 편도 0.02%
- 중복 포지션: 종목별 한 번에 하나만 허용
- 숏 결과: 현물 가격을 사용한 가상 숏 성과이며 현물 계정에서 직접 실행 가능하다는 뜻이 아님

## 검증 절차

1. 지표 공식 대조
   - RSI, EMA, ATR, MACD, SuperTrend, 완료봉 판정을 독립 기준값과 비교한다.
   - 재현되는 오류가 있을 때만 테스트를 먼저 추가하고 수정한다.

2. 고정 데이터 스냅샷
   - Binance 공식 API에서 1h/4h/8h/12h/1d/1w/1M 확정봉을 수집한다.
   - 누락 구간, 비정상 OHLC, 0 거래량과 SHA-256을 기록한다.

3. 시간순 홀드아웃 백테스트
   - 전체 기간의 앞 60%와 마지막 40%를 시간으로 분리한다.
   - 고정 규칙이므로 홀드아웃을 보고 임계값을 재최적화하지 않는다.
   - 마지막 40%를 주 결과로 사용한다.

4. 결과 지표
   - 진입 신호 수, 실제 체결 수, 승률, Profit Factor, 평균 기대 R, 누적 R, 최대 낙폭을 보고한다.
   - 롱·숏 및 종목별 결과도 분리한다.
   - 30거래 미만은 `표본 부족`으로 표시한다.

5. 최소 수정 및 재검증
   - 공식 오차, 미래 데이터 누수, 잘못된 체결 처리 또는 공개 페이지 오류만 수정한다.
   - 성과가 음수이면 수익이 좋아 보이도록 임계값을 맞추지 않고 `전략 우위 미확인`으로 표시한다.

## 완료 조건

- 모든 기존 회귀 테스트와 신규 백테스트 테스트 통과
- 비용 포함 마지막 40% 홀드아웃 보고서 생성
- 390px 모바일에서 기존 검색·즐겨찾기·분석 흐름과 사용자 상태 유지
- 공개 Vercel 페이지에서 변경 내용과 콘솔 오류 여부 확인
- 백테스트 관련 변경만 선별 커밋·푸시
