# 실시간 외부 데이터 및 본인 전용 MEXC 계정 구현 계획

## 현재 확인된 상태

- 2026-09-10 공개 웹페이지와 로컬 소스를 확인했다. 저장소는 `cassmania/upbit-analyzer`, 작업 폴더는 `E:\AI 관련 자료\클로드\upbit_analyzer`이다.
- 작업 시작 시 `main`에 미커밋 변경은 없었다.
- `app.js`의 화면 및 브리핑 생성부와 `index.html` 안내에 데이터 확인 불가 문구가 고정되어 있다. 실제 공급업체 연동을 추가해야 한다.
- 현재 MEXC 중계는 공개 현물 시세만 허용한다. 로그인, 계정별 접근 제어, 비공개 잔고 조회 기능은 없다.
- `vercel.json`은 모든 API에 공개 CORS 헤더를 설정한다. 개인 API는 이 정책과 공개 캐시에서 분리해야 한다. CORS 자체는 인증 수단이 아니다.
- 현재 단계는 조사 및 계획 완료이며, 기능 구현·계정 연결·배포는 아직 수행하지 않았다.

## 목표 화면

1. 공개 분석 화면: 기존 차트·즐겨찾기를 유지하고 각 외부 지표에 값, 출처, 대상 거래소, 기준 시각, 갱신 상태를 표시한다.
2. 내 MEXC: 로그인한 허용 사용자만 현물 잔고, 선물 자산, 활성 포지션, 미체결 주문을 확인한다. 포지션은 롱/숏, 수량, 진입가, 미실현 손익, 제공되는 청산가를 표시한다.
3. 첫 연결 범위는 조회 관리로 한다. 주문 실행·취소·자금 이동은 별도 범위 확정 및 구현이 필요하다.
4. 다른 방문자는 공개 분석 기능을 계속 사용하며, 개인 API를 직접 호출하더라도 계정 정보는 받을 수 없다.

## 데이터별 연결 방안과 실제 한계

| 항목 | 연결 방안 | 표시 및 검증 기준 |
| --- | --- | --- |
| CVD | 공식 체결 API의 매수/매도 주도 체결량 누적 | 시작 시각, 집계 구간, 수량/대금 단위, 거래소·현물/선물 표시. OHLCV 방향으로 임의 대체하지 않음. 재연결 누락·중복 검증 |
| 청산맵 | CoinGlass 공식 API | 모델 추정 청산 밀집도와 실제 발생한 청산을 구분. 사용 가능한 요금제·거래쌍 확인 |
| 거래소 순유입, MVRV, SOPR | Glassnode 등 공식 온체인 API | 지원 자산·지표·해상도 확인. 온체인 기준 시각과 조회 시각을 구분. 순유입은 유입-유출 및 단위 검증 |
| 고래 이동 | Whale Alert 등 주소 라벨 제공 API | 체인·트랜잭션·수량·주소 라벨·확인 시각 표시. 거래소 대량 체결을 온체인 고래 이동으로 부르지 않음 |
| 락업·언락 | Tokenomist 등 일정 API | 토큰 식별자·네트워크 확인, 일정·수량·유통량 대비 비율. 단순 비유통량을 락업량으로 대체하지 않음 |
| 뉴스 | 공식 공지 RSS 또는 사용 허가된 뉴스 API | 제목·발행 시각·원문 링크·출처 중심. 본문 HTML을 실행하지 않으며 동명 코인 혼동 방지 |

실시간은 공급업체가 제공하는 가장 최근의 데이터라는 뜻이다. 일 단위 온체인 값을 초 단위 체결값처럼 표시하지 않는다. 실패·미지원·인증 미설정·요금제 제한·오래된 값은 개별 상태로 표시하고 다른 지표 갱신은 계속한다. 모든 코인을 모든 지표에서 지원한다고 보장하지 않는다.

CoinGlass 청산맵과 Tokenomist 등은 API 이용권 확인이 필요하다. 공개 사이트 재배포 권한을 확인하기 전에는 유료 데이터는 본인 인증 영역에서만 사용하도록 설계한다. 구독 비용은 이번 계획에서 결제하거나 승인한 것으로 간주하지 않는다.

## 본인 전용 보안 구조

브라우저 → 로그인 세션 확인 → 서버의 허용 사용자 ID 확인 → MEXC 요청 서명 → 최소 계정 데이터 반환

- 관리형 인증 서비스의 검증된 로그인과 서버 세션을 사용한다. Supabase Auth를 우선 후보로 하며 실제 프로젝트 및 로그인 수단을 확인한다.
- 소유자 ID는 서버에 사전 등록한다. 첫 방문자를 관리자로 자동 등록하거나 브라우저가 보낸 사용자 ID를 신뢰하지 않는다. 미설정이면 접근을 거부한다.
- HttpOnly·Secure·SameSite 쿠키와 CSRF/Origin 검증을 적용하고, 로그아웃 후 세션 무효화를 확인한다. 상태 변경은 GET으로 수행하지 않는다.
- 모든 개인 API가 세션과 허용 사용자 ID를 독립적으로 검증한다. 화면을 숨기는 것만으로 보호하지 않는다.
- MEXC API Key/Secret은 채팅·HTML·프런트엔드 번들·localStorage·Git에 넣지 않는다. 읽기 권한만 가진 키를 서버 보안 설정으로 연결한다.
- 자격증명을 저장해야 한다면 서버에서 AES-256-GCM으로 암호화하고 암호화 키는 저장소와 분리한다. 브라우저 전송에는 HTTPS를 사용한다. 잔고·포지션은 기본적으로 DB에 저장하지 않는다.
- 개인 API는 `Cache-Control: private, no-store`를 사용한다. 공개 CDN 캐시·공개 브리핑·공유 파일·서버 로그에 계정 정보와 서명 요청을 포함하지 않는다.
- 서버 중계는 허용된 거래소 도메인과 읽기 경로로 제한한다. 외부 오류는 키·서명·계정 데이터가 제거된 오류 코드로 변환한다.
- API 응답의 개인정보를 최소화하고 외부 텍스트는 HTML 이스케이프한다. 요청 속도 제한과 타임아웃을 둔다.
- MEXC 키에 IP 제한을 적용할 때는 배포 서버의 고정 송신 IP 사용 가능 여부부터 확인한다.
- SQL이 필요하면 `supabase/migrations/001_*.sql`부터 순서대로 생성하고 RLS를 적용한다.

## 변경 예정 파일과 단계

### 1단계: 비공개 계정 경계

- 인증·개인 MEXC API·별도 계정 UI를 신규 모듈로 추가한다.
- `vercel.json`에서 공개 API와 개인 API의 헤더를 분리한다.
- 비로그인/다른 사용자 차단과 암호화·캐시 방지 테스트를 먼저 통과시킨다.
- 실제 키와 본인 로그인은 보안 설정 경로에서 사용자 연결이 필요하다.
- 사용자가 이 기능의 작동을 확인한 후 다음 단계로 넘어간다.

### 2단계: 무료 실시간 근거

- CVD 체결 수집 및 출처가 명확한 뉴스 연결을 추가한다.
- 화면과 복사 브리핑이 같은 데이터 객체를 사용하도록 `app.js`의 해당 출력 지점을 수정한다.
- 기존 분석 엔진·검색·즐겨찾기·완료봉 처리 동작은 유지한다.
- 체결 누락·거래소 전환·갱신 실패·모바일 화면을 검증하고 사용자 확인을 받는다.

### 3단계: 전문 데이터 공급업체

- 제공된 API 이용권에 맞춰 청산맵·온체인·고래·언락 어댑터를 순서대로 연결한다.
- 키 미설정 상태는 연결 완료로 보고하지 않는다. 공급업체가 반환한 실제 응답을 확인한다.
- 웹소켓 상시 수집이 필요한 공급업체는 Vercel 단기 요청 함수에 무리하게 넣지 않고 별도 지속 수집 서비스가 필요한지 검토한다.

### 4단계: 배포 검증

- 기존 회귀 테스트, 인증 실패·권한 위조·캐시 격리·로그아웃 테스트 수행.
- 본인 세션과 비로그인/별도 사용자 세션에서 같은 URL을 확인하고, 개인 API 직접 요청도 검증.
- 모바일·데스크톱에서 공개 차트 및 계정 패널 작동 확인.
- 공개 배포 URL과 정적 파일에 비밀값·개인 잔고가 포함되지 않았는지 확인.
- 실제 계정 연결 성공과 공급업체별 성공/차단 항목을 구분해 보고.

## 구현 전 필요한 승인 및 설정

- 기존 서비스에 로그인·서버 인증·외부 데이터 계층을 추가하는 구조 변경에 대해 사용자 AGENTS.md 2번에 따른 사전 승인이 필요하다.
- 본인 로그인 계정 식별과 인증 서비스 설정, MEXC 읽기 전용 키 연결이 필요하다. 키 원문은 채팅으로 요청하지 않는다.
- 전문 데이터 API 키·이용권과 공개 재배포 범위를 확인해야 전체 항목을 실제 연결할 수 있다.

## 확인한 공식 자료

- MEXC API: https://www.mexc.com/en-GB/mexc-api
- MEXC API 키 안내: https://www.mexc.com/en-GB/announcements/article/mexc-sub-account-api-key-setup-manual-17827791534686
- Binance 체결 스트림: https://github.com/binance/binance-spot-api-docs/blob/master/web-socket-streams.md
- CoinGlass 청산맵: https://docs.coinglass.com/reference/liquidation-map
- Glassnode 온체인 지표: https://docs.glassnode.com/basic-api/endpoints/indicators
- Glassnode 자산/지표 메타데이터: https://docs.glassnode.com/basic-api/metadata
- Whale Alert: https://developer.whale-alert.io/api-account/documentation
- Tokenomist: https://docs.tokenomist.ai/api-documents/introduction
