# 코인 검색·즐겨찾기 구현 계획

## 목표

기존 거래소·코인 선택 흐름을 유지하면서, 사용자가 코인을 검색하고 검색 결과에서 즐겨찾기를 등록·해제할 수 있게 한다.

## 범위

1. 검색 결과에 즐겨찾기 별표 버튼을 표시한다.
2. 별표 클릭 시 현재 거래소별 마켓을 `localStorage`에 저장하고 즉시 목록을 갱신한다.
3. 검색어가 비어 있을 때 즐겨찾기 코인을 먼저 보여주고, 검색 중에는 일치 결과를 보여준다.
4. 거래소 전환 시 거래소별 즐겨찾기를 분리한다.
5. 새로고침 후에도 즐겨찾기 상태를 유지한다.

## 보존 조건

- 기존 `select#market`과 자동 분석 동작은 유지한다.
- 검색만으로 현재 분석 종목이 바뀌지 않는 기존 안전장치를 유지한다.
- 서버·DB를 추가하지 않고 브라우저별 로컬 저장만 사용한다.
- 모바일에서 버튼을 누르기 쉬운 크기와 키보드 접근성을 유지한다.

## 검증

- JavaScript 정적 구문 검사
- 기존 회귀 테스트
- 검색어 입력, 별표 등록/해제, 새로고침 유지, 거래소별 분리 확인
- Impeccable 탐지기 1회 실행
