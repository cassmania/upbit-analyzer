# 코인 검색·즐겨찾기 개선 구현 계획

## 목표

1. 검색창에 코인 심볼이나 이름을 끝까지 입력할 수 있고, 검색 결과에서 바로 즐겨찾기를 등록하거나 코인을 선택해 분석한다.
2. 사용자가 마지막으로 이용한 거래소와 코인을 복원하여, 재접속했을 때 거래소별 즐겨찾기가 초기화된 것처럼 보이지 않게 한다.

## 원인

- 즐겨찾기 자체는 `localStorage`에 거래소별로 저장되지만, 재접속 시 거래소가 항상 바이낸스로 초기화되어 다른 거래소의 즐겨찾기 개수가 0으로 보일 수 있다.
- 입력 중 정확히 일치하는 짧은 심볼(예: MEXC의 `F`)이 발견되면 즉시 분석을 시작하고 검색어를 지워, `FET`처럼 더 긴 심볼을 끝까지 입력할 수 없다.

## 구현 범위

1. 마지막 거래소와 코인을 브라우저 로컬 저장소에 저장하고 다음 접속 때 복원한다.
2. 기존 거래소별 즐겨찾기 저장 키는 그대로 유지하여 이미 등록한 즐겨찾기를 잃지 않는다.
3. 입력 중에는 분석을 자동 실행하지 않고 검색 결과와 즐겨찾기 별표를 유지한다.
4. `Enter`를 누르거나 검색 결과의 코인명을 클릭하면 정확히 일치하는 코인 또는 첫 검색 결과를 바로 분석한다.
5. 검색 결과에서 별표를 누르는 기존 즐겨찾기 등록·해제 기능은 유지한다.

## 보존 조건

- 거래소 전환, 시간봉, 자동 갱신, 차트, V4.1 브리핑 UI는 변경하지 않는다.
- 서버·DB를 추가하지 않고 비민감 UI 설정만 브라우저 `localStorage`에 저장한다.
- 검색 중 불완전한 글자나 짧은 심볼로 분석이 자동 시작되지 않게 입력 이벤트에서는 코인을 선택하지 않는다.
- 모바일과 키보드 사용자는 검색 결과 클릭 또는 `Enter`로 같은 동작을 할 수 있게 한다.

## 검증

- JavaScript 정적 구문 검사와 기존 전체 회귀 테스트
- `FET`처럼 짧은 심볼과 접두사가 겹치는 검색어를 끝까지 입력하고 별표를 누를 수 있는지 확인
- 정확한 심볼 또는 부분 검색 후 `Enter` 분석 확인
- 즐겨찾기 등록 후 새로고침 유지 확인
- MEXC 같은 비기본 거래소와 마지막 코인이 재접속 후 복원되는지 확인
- 배포 후 캐시를 우회하여 실제 Vercel 화면에서 재검증
