# 코인분석스킬 V4.1 이식 계획

## 목표

기존 업비트 분석기의 UI, 거래소 전환, 실시간 시세, 차트, 지지·저항과 알림 상태를 보존하면서 `코인분석스킬 4.1v`의 데이터 계약과 출력 규칙을 이식한다.

## 변경 범위

1. 기존 V3 구조 엔진 위에 V4.1 호환 계층을 추가한다.
2. Stochastic을 정확한 `(14,3,3)` 평활 방식으로 교정한다.
3. 이동평균 표기를 20/60/120/200기간으로 통일하고 시간봉에서 `일선`이라고 부르지 않는다.
4. 브리핑과 화면을 PART 0~5 구조로 갱신한다.
5. USDT.D, 파생상품, 온체인, 뉴스·언락의 출처·시각·가용성 한계를 명시한다.
6. 실제 VPVR와 OHLCV 범위분배 근사 VPVR를 구분한다.
7. 모멘텀 지표 중복 점수화를 금지하고 비용·레버리지·무효화 규칙을 표시한다.
8. 기존 테스트와 V4.1 전용 회귀 테스트를 모두 실행한다.

## 보존 항목

- 현재 UI 레이아웃과 사용자 조작 방식
- 업비트·빗썸·바이낸스·MEXC 전환
- 완료봉 분리와 합성봉 검증
- 레벨 엔진, 시그널 엔진, 차트와 알림 상태
- 화이트리스트 방식 서버리스 프록시

## 데이터 한계

- 브라우저에서 검증 가능한 무료 원자료가 없는 청산맵·CVD·고래·MVRV·SOPR·뉴스·언락은 수치를 만들지 않는다.
- 현재 CoinGecko 프록시가 제공하는 USDT.D는 현재값만 사용하며 시계열 방향을 추정하지 않는다.
- 공개 OHLCV 기반 VPVR은 `근사 VPVR`로만 표시한다.
- 현물 분석에 붙는 Binance USDT-M 펀딩·OI는 보조자료이며 시장 종류가 다름을 표시한다.

## 검증 단계

- [x] 배포본 및 로컬 기준선 확인
- [x] 기존 구현과 V4.1 요구사항 차이 분석
- [x] 변경 전 백업
- [x] V4.1 호환 계층과 TA 교정
- [x] 화면·브리핑·문서 갱신
- [x] 기존 및 신규 회귀 테스트
- [x] 로컬 브라우저 모바일·데스크톱 검증
- [x] Git diff 및 배포 상태 확인
