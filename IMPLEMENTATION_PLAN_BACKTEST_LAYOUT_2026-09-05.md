# upbit-analyzer 백테스트 재검증 및 컨트롤 정렬 개선 계획

## 목표

배포 중인 `upbit-analyzer`의 지표 계산과 신호 성과를 현재 데이터로 다시 검증하고, 재현되는 오류와 불필요한 처리 비용을 최소 범위로 개선한다. 동시에 거래소 선택, 코인 검색, 즐겨찾기, 종목 선택, 시간봉 및 실행 버튼을 일정한 규격의 반응형 컨트롤 영역으로 정렬한다.

## 보존해야 할 기존 동작

- 확정봉만 사용하는 `CandleUtils.completed(...)` 데이터 흐름
- `TAEngine → LevelEngine → SignalEngine` 계산 순서
- 거래소별 즐겨찾기 키 `upbit-analyzer:favorites:<exchange>`
- 마지막 거래소·코인 복원 키 `upbit-analyzer:preferences:v1`
- 검색 결과 또는 즐겨찾기 코인을 누르면 즉시 분석하는 흐름
- 업비트·빗썸·바이낸스·MEXC 전환과 기존 API 어댑터

## 1단계: 기준선 수집

1. 현재 Git 상태와 공개 배포 파일을 비교한다.
2. 기존 회귀 테스트와 구문 검사를 먼저 실행한다.
3. 현재 고정 데이터로 비용 포함 홀드아웃 백테스트를 재실행한다.
4. 승률만 보지 않고 거래 수, Profit Factor, 평균 R, 누적 R, 최대낙폭을 함께 기록한다.

## 2단계: 정확도 및 오류 감사

1. 진행 중인 봉이 지표·신호에 섞이지 않는지 확인한다.
2. 다음 봉 진입, 손절 우선, 수수료·슬리피지, 최대 보유 시간 처리를 테스트한다.
3. RSI·EMA·ATR·MACD·SuperTrend 계산 결과를 기존 독립 테스트로 재검증한다.
4. 공식 오류, 미래 데이터 누수, 잘못된 체결 또는 상태 복원 오류가 재현될 때만 수정한다.

## 3단계: 성능 개선

1. 동일 데이터에 대한 중복 지표 계산과 불필요한 DOM 갱신을 점검한다.
2. 검색 입력 중 전체 목록을 반복 처리하는 구간은 사용자 체감 지연이 없도록 제한 또는 재사용한다.
3. 자동 갱신 중 동일 요청 중첩과 오래된 응답 덮어쓰기를 방지한다.
4. 계산 결과를 바꾸는 최적화는 전후 동등성 테스트를 추가한다.

## 4단계: 컨트롤 정렬

PC에서는 다음 3개 행으로 규격화한다.

1. 거래소 선택 행
2. 검색·즐겨찾기 탭, 입력창 또는 즐겨찾기 목록, 종목 선택 행
3. 시간봉, 분석, 자동 갱신, 브리핑, 알림, 갱신 주기 행

스마트폰에서는 각 행이 화면 폭에 맞춰 한 열 또는 안전한 줄바꿈으로 바뀌도록 하며, 입력·선택·버튼의 높이를 동일하게 맞춘다. 검색 결과 팝업은 해당 입력 영역 폭을 기준으로 표시한다.

## 완료 조건

- 모든 기존 및 추가 회귀 테스트 통과
- 현재 데이터 백테스트 보고서 갱신
- 1920px PC와 390px·360px 모바일에서 가로 넘침 0px
- 검색·즐겨찾기·종목 선택의 시작선과 높이 정렬
- 검색, Enter 분석, 즐겨찾기 저장·복원, 거래소 전환 동작 확인
- 공개 Vercel 주소에서 새 버전과 실제 상호작용 확인

## 결과 해석 원칙

백테스트의 `신뢰도`는 미래 적중 확률이 아니라 지정한 기간과 체결 가정에서 나온 과거 표본 성과다. 30거래 미만은 표본 부족으로 표시하고, Profit Factor와 평균 R이 기준을 충족하지 못하면 `전략 우위 미확인`으로 보고한다.
