# 업비트 분석기 정확도·안정성 개선 계획

## 목표

현재 화면과 거래소 지원 범위는 유지하면서, 지표·지지저항·진입 신호가 미완성 봉이나 잘못된 시간축 때문에 흔들리는 문제를 줄인다. 결과는 예측 확률이 아니라 재현 가능한 조건부 분석으로 표시한다.

## 확인된 문제

1. 업비트 계열 캔들의 `timestamp`를 봉 시작 시각으로 사용해 합성 8시간·12시간봉 경계가 실제 거래소 경계와 달라질 수 있다.
2. 차트 표시용 진행 봉과 지표 계산용 완료 봉이 분리되지 않아 새로고침 중 RSI·VPVR·신호가 흔들린다.
3. ATR 공개 함수가 단순평균을 사용해 Wilder 표준값과 다르다.
4. VPVR이 한 캔들의 거래량 전부를 대표가격 한 칸에 넣어 장대봉의 가격대별 거래량을 과도하게 집중시킨다.
5. RSI·스토캐스틱·CCI처럼 서로 비슷한 모멘텀 신호가 각각 한 표씩 반영돼 수렴도가 과장될 수 있다.
6. 분석 기준 시각과 완료봉 사용 여부가 화면에 명확히 드러나지 않는다.

## 구현 순서

### 1단계 — 시간축과 완료봉

- 업비트·빗썸은 `candle_date_time_utc`, 바이낸스·MEXC는 kline open time을 봉 시작 시각으로 정규화한다.
- 합성봉은 UTC 기준 절대 경계로 묶고, 구성 봉이 모두 끝난 합성봉만 분석에 사용한다.
- 차트에는 진행 봉을 유지하되 TA·레벨·신호에는 완료봉만 전달한다.
- 시간축·완료 여부를 검증하는 독립 테스트를 추가한다.

### 2단계 — 계산 정확도

- ATR을 Wilder RMA 방식으로 통일한다.
- VPVR 거래량을 캔들의 고가~저가와 겹치는 각 가격 구간에 비례 배분한다.
- TA 엔진과 레벨 엔진의 VPVR 계산을 같은 규칙으로 맞춘다.
- 모멘텀 계열은 하나의 그룹 점수로 묶어 중복 가중을 완화한다.

### 3단계 — 설명 가능성과 안전성

- 화면에 거래소, 시장(현물/선물 보조), 데이터 갱신 시각, 지표 기준 완료봉 시각을 표시한다.
- 펀딩·미결제약정은 바이낸스 USDT-M 보조자료임을 명시하고 미상장 시 `데이터 없음`을 유지한다.
- 타점은 돌파·재시험·무효화 조건과 함께 표시하며 정확도나 수익을 보장하는 표현은 쓰지 않는다.

### 4단계 — 검증과 배포

- 기존 86개 테스트와 신규 회귀 테스트를 모두 실행한다.
- 실제 API 표본으로 시간 정렬, 완료봉 제외, 지지<현재가<저항 불변식을 검사한다.
- 로컬 브라우저에서 콘솔 오류와 주요 거래소 전환을 확인한다.
- `main`에 커밋·푸시해 Vercel 자동 배포를 실행하고 공개 URL의 버전과 API 응답을 재검증한다.

## 복구 지점

- 로컬: `E:\AI 관련 자료\챗지피티\backups\upbit-analyzer\2026-08-17_원본_개선전`
- GitHub: `codex/backup-pre-upgrade-2026-08-17`
- 원본 커밋: `348b96379bbb094383a94c1cbc1ed6e7dc76fa34`
