# 백테스트 재현성·지정가 체결 개선 계획

1. 수정 전 파일과 데이터를 백업하고 기존 테스트 및 동일 스냅샷 기준선을 실행한다.
2. 실행 시각에 따라 바뀌는 홀드아웃 경계를 저장 데이터 기간으로 고정한다. 학습과 시험을 포지션 없이 독립 실행한다.
3. 진입 전에 발생했을 수 있는 고가를 같은 봉의 익절로 인정하는 오류와, 손절가를 넘어선 신규 지정가 진입을 검증·수정한다.
4. 파일 해시와 OHLC·시간 무결성을 검사하고, 반복되는 시간봉 입력 변환은 계산값을 보존하며 재사용한다.
5. 같은 데이터로 전후 성과·속도를 비교하고 공개 요약을 보고서와 자동 동기화한다. 검색·즐겨찾기·레이아웃은 유지한다.

## 범위와 해석

Binance 현물 5종의 고정 규칙을 검증한다. 가상 숏은 현물 주문 성과가 아니며, 업비트·빗썸·MEXC 실거래 성공률로 일반화하지 않는다. 승률·PF·평균 R·청산 손익 기준 최대낙폭을 함께 보고하고 수익 우위가 없으면 그대로 공개한다.

## 완료한 계산 및 검증

- 원자료는 2024-01-01부터 2026-09-04 19:00 UTC 미만의 기존 스냅샷을 유지했다. 원자료 해시·유한값·OHLC·시간 순서를 검사했다.
- 수정 전 이번 실행: 홀드아웃 86거래, 승률 41.9%, PF 0.769, 평균 -0.183R. 실행 시각을 사용하던 분할이므로 과거 공개 87거래와 달라지는 재현성 문제를 확인했다.
- 수정 후 저장 기간 고정·독립 구간: 87거래, 승률 39.1%, PF 0.690, 평균 -0.256R, 누적 -22.289R, 청산 기준 최대낙폭 31.479R. 전략 우위 미확인이다.
- 롱 35거래 PF 0.874, 가상 숏 52거래 PF 0.590. 체결 가정과 구간 처리가 바뀌었으므로 이 차이를 수익성 개선으로 해석하지 않는다.
- 입력 변환 캐시 검증: BTC 500시간 소구간에서 신호·거래·차단 사유 전부 일치. 두 측정 333.9→272.2ms, 380.2→281.0ms. 전체 화면 로딩 속도나 전체 기간 실행시간 개선율이 아니다.
- 구간 밖 미래 데이터를 잘라내도 해당 구간 결과가 같은지 검증했다. 롱·숏 모두 진입 전 고가·저가를 당봉 익절로 인정하지 않는 회귀 테스트를 추가했다.
- 화면용 요약은 보고서와 동시에 생성한다. 요약 로드 실패 시 예전 승률을 표시하지 않고 확인 불가로 안내한다.

## 재현 명령

`node backtest/signal_backtest.js`는 저장된 데이터로 재실행한다. `node test_backtest_reproducibility.js`는 캐시 동등성과 미래 입력 차단을 검증한다. `node test_signal_backtest.js`는 지정가·손절·비용 계산을 검증한다.
